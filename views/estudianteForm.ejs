<!DOCTYPE html>
<html>
<head>
    <title>Gestión de Estudiantes</title>
</head>
<body>

<form id="formEstudiante">
    <h3>Crear un estudiante</h3>
    <input type="text" name="nombre" placeholder="Nombre" required>
    <input type="text" name="edad" placeholder="Edad" required>
    <input type="text" name="correo" placeholder="correo" required>
    <button type="submit">Guardar</button>
</form>

<script>
    document.getElementById('formEstudiante').addEventListener('submit', async (e) => {
        e.preventDefault();
        const data = {
            nombre: e.target.nombre.value,
            edad: e.target.edad.value,
            correo: e.target.correo.value
        };

        const res = await fetch('/estudiantes', {  //misma ruta del crud
            method: 'POST',
            headers: { 'content-Type': 'application/json' },
            body: JSON.stringify(data)
        });

        const result = await res.json();
        alert("Estudiante creado: " + result.nombre);
    });
</script>

<!-- Obtener Todos los Estudiantes -->
<form id="obtenerTodos">
    <h3>Obtener Todos</h3>
    <button type="submit">Ver todos</button>
</form>

<!-- Obtener Estudiante por ID -->
<form id="obtenerUno">
    <h3>Obtener por el id</h3>
    <input name="id" type="number" placeholder="ID" required>
    <button type="submit">Buscar</button>
</form>

<!-- Actualizar Estudiante -->
<form id="actualizarEstudiante">
    <h3>Actualizar Estudiante</h3>
    <input name="id" type="number" placeholder="ID" >
    <input name="nombre" placeholder="Nuevo nombre">
    <input name="edad" type="number" placeholder="Nueva edad">
    <input name="correo" type="email" placeholder="Nuevo correo">
    <button type="submit">Actualizar</button>
</form>

<!-- Eliminar Estudiante -->
<form id="eliminarEstudiante">
    <h3>Eliminar Estudiante</h3>
    <input name="id" type="number" placeholder="ID" required>
    <button type="submit">Eliminar</button>
</form>

<script>
// Crear Estudiante
// document.getElementById('crearEstudiante').onsubmit = async function(e) {
//     e.preventDefault();
//     const data = Object.fromEntries(new FormData(this));
//     const res = await fetch('/estudiantes', {
//         method: 'POST',
//         headers: {'Content-Type': 'application/json'},
//         body: JSON.stringify(data)
//     });
//     const result = await res.json();
//     alert(result.error ? result.error : 'Estudiante creado correctamente');
// };

// Obtener Todos
document.getElementById('obtenerTodos').onsubmit = async function(e) {
    e.preventDefault();
    const res = await fetch('/estudiantes');
    const result = await res.json();
    alert(JSON.stringify(result, null, 2));
};

// Obtener por ID
document.getElementById('obtenerUno').onsubmit = async function(e) {
    e.preventDefault();
    const id = this.id.value;
    const res = await fetch('/estudiantes/' + id);
    const result = await res.json();
    alert(result.error ? result.error : JSON.stringify(result, null, 2));
};

// Actualizar
document.getElementById('actualizarEstudiante').onsubmit = async function(e) {
    e.preventDefault();
    const id = this.id.value;
    const data = Object.fromEntries(new FormData(this));
    delete data.id;
    const res = await fetch('/estudiantes/' + id, {
        method: 'PUT',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(data)
    });
    const result = await res.json();
    alert(result.error ? result.error : 'Estudiante actualizado correctamente');
};

// Eliminar
document.getElementById('eliminarEstudiante').onsubmit = async function(e) {
    e.preventDefault();
    const id = this.id.value;
    const res = await fetch('/estudiantes/' + id, { method: 'DELETE' });
    const result = await res.json();
    alert(result.error ? result.error : result.mensaje);
};
</script>

</body>
</html>
